# ARC Runner Configuration
# Copy this file to arc-config.env and update with your values
# DO NOT commit arc-config.env to git (it's in .gitignore)

# GitHub Personal Access Token with repo and admin permissions
GITHUB_TOKEN=your_github_token_here

# GitHub Repository URL
GITHUB_URL=https://github.com/ChidhagniConsulting/gitops-semgrep

# Usage:
# 1. Copy this file: cp arc-config.env.example arc-config.env
# 2. Edit arc-config.env with your actual token
# 3. Source the config: source arc-config.env
# 4. Run the script: ./manage-arc-runners.sh status
